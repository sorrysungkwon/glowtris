# Antigravity Rules
* Read this first. Then `ROBOT.md`. Then `TODO.md`.
* NO SECRETS IN GIT.
* Put secrets in `CREDENTIALS.md` only.
* Use `<PLACEHOLDER>` in docs.
* See secret? Delete fast. Add placeholder. Commit fix.
* User say "sync"? You do:
  * Read `AGENTS.md`, `ROBOT.md`, `TODO.md`, `WALKTHROUGH.md`.
  * Check `git log -10`.
  * Tell user: branch, commit, changes, next task.
  * Find bad doc info? Tell user.
* `ROBOT.md` have shared rules.
* `README.md` for human only. No rules there.
* Keep all specs in `TODO.md` and `WALKTHROUGH.md`.